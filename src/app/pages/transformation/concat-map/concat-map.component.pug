p Subscribe to provided observable when previous completes, emit values.
p concatMap() объединяет все сообщения нового потока с сообщениями потоков, с которыми уже работает. И перед запуском нового потока дожидается завершения предыдущего. Используется тогда, когда нам важен порядок обработки сообщений, и важно все эти сообщения получить.

.stream-block
  p Source 1
  app-draw-stream([stream]="source1")
  p Source 2
  app-draw-stream([stream]="source2")
  p Result 1
  app-draw-stream([stream]="result1")

.stream-block
  p Source 1
  app-draw-stream([stream]="source3")
  p Source 2
  app-draw-stream([stream]="source4")
  p Result 1
  app-draw-stream([stream]="result2")

.stream-block
  p Source 1
  app-draw-stream([stream]="source5")
  p Source 2
  app-draw-stream([stream]="source4")
  p Result 1
  app-draw-stream([stream]="result3")

.stream-block
  p Source 1
  app-draw-stream([stream]="source6")
  p Source 2
  app-draw-stream([stream]="source7")
  p Result 1
  app-draw-stream([stream]="result4")
