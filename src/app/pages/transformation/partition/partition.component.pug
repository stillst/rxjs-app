p Split one observable into two based on provided predicate.

.stream-block
  p Source
  app-draw-stream([stream]="source1")
  p Result 1
  app-draw-stream([stream]="result1")
  p Result 2
  app-draw-stream([stream]="result2")
